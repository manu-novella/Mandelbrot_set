function setup() {
  canvas = createCanvas(600, 600)
  pixelDensity(1)
  background(0)

  iterate_complex_plane()
  noLoop

  //timestamp = Date.now().toString()
  //saveCanvas(canvas, "outputs/" + timestamp, "png")
}


function draw() {
  
}
